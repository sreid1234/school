#indef EVENT_h
#define EVENT_h


class Event
{

    public:
        void change_Time(time);
        void change_Date(date);
        void change_Name(name);
        enum compareTo(date, otherDate)
        struct get_event() const;
    protected:
    private:
};

#endif
 
 

